[project]
name = "Spring2025_ANN_tutorial"
version = "1.0.0"
dependencies = [
        "mordred==1.2.0",
        "pandas==1.3.5",
        "rdkit==2023.9.6",
        "rdkit_pypi==2022.9.5",
        "torch==2.2.1",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
